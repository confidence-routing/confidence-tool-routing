# confidence-tool-routing
Research implementation of confidence-based tool routing for LLM agents: gates tool calls on internal/external confidence signals instead of always/never calling tools. Includes a custom eval harness (CPST, ECE, routing precision/recall) and experiments on cross-tool calibration transfer.
